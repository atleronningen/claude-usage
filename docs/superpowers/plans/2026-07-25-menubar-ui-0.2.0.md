# Menylinje-UI «ren tekst, skarpere» (1b) — implementasjonsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementere design-handoffen i `docs/design_handoff_menubar_1b/` — forbruksdata inn i menyen som tekstmålere med nullstillingstidspunkt, kortere menylinjetittel, tydeligere feiltilstand. Kulminerer i release `v0.2.0`.

**Architecture:** Alt gjøres med vanlige `rumps.MenuItem`-strenger — ingen `NSAttributedString`, ingen custom `NSView`, ingen nye avhengigheter. Menyen bygges once i `__init__`; `refresh`/`_show_error` oppdaterer bare `title` og `hidden` på eksisterende, forhåndsopprettede `MenuItem`/separator-objekter.

**Tech Stack:** Python 3.9, `rumps` 0.4.0 (uendret versjon), `pytest`.

## Global Constraints

- Ingen nye avhengigheter. Alt innenfor `rumps.MenuItem` (+ `rumps.rumps.SeparatorMenuItem` for skjulbare separatorer — se «Teknisk avklaring» under).
- Meny bygges **én gang** i `__init__`; `refresh`/`_show_error` oppdaterer kun `title`/`hidden` — aldri gjenoppbygging (lukker menyen for brukeren hvis åpen).
- Terskelen for `!`-markering skal leses fra `claude_usage.notifier.THRESHOLD_PERCENT` (verdien er `90`), ikke hardkodes på nytt.
- Målere: 10 celler, fylt `▰` (U+25B0), tom `▱` (U+25B1). `filled = max(0, min(10, round(percent / 10)))`. Format: `f"{label:<7}{bar} {percent}%{'!' if percent >= threshold else ''}"`.
- Tittelseparator og metaseparator: `" · "` (mellomrom + U+00B7 + mellomrom) — konsekvent overalt, aldri bindestrek.
- Nullstillingsklokke: 24-timers format, **lokal tid** (API returnerer UTC med offset, f.eks. `2026-07-25T11:10:00.845131+00:00` — verifisert mot faktisk API-svar i denne økten).
- Ukedagsforkortelser: `man tir ons tor fre lør søn` (indeks 0=mandag, matcher `datetime.weekday()` direkte).

## Avvik fra det opprinnelige handoff-dokumentet (avklart med Atle i denne økten)

Disse punktene avviker fra `docs/design_handoff_menubar_1b/README.md` / skjermbildene, etter eksplisitt avklaring:

1. **«Oppdater nå» fjernes helt** fra menyen (appen oppdaterer seg selv hvert 60. sekund uansett — ingen manuell trigger trengs). Dette var opprinnelig i handoffen (med tidspunkt til høyre), men er bevisst tatt bort.
2. **Footeren viser tidspunkt + versjon i motsatt rekkefølge av skjermbildene**, med `·` i stedet for bindestrek: `Oppdatert 14:03 · App v0.1.0` (før første vellykkede henting: bare `App v0.1.0`). Dette er nå footerens eneste jobb — den er den ene, konsekvente plassen som viser «sist oppdatert» i normaltilstand.
3. **Oppskrift-dialogens siste steg endres** (kan ikke lenger referere til «Oppdater nå»): «6. Lagre filen — appen henter automatisk på nytt innen ett minutt» (var: «Lagre filen, og klikk "Oppdater nå" i menyen»).

## Teknisk avklaring (funnet ved å lese `rumps` 0.4.0-kildekoden direkte i denne økten — ikke i handoff-dokumentet)

- **`rumps.MenuItem` har en `hidden`-property** (`.hidden = True/False`, wrapper rundt `NSMenuItem.setHidden_`) — tilgjengelig i denne rumps-versjonen, brukes til å skjule/vise datalinjer og feil/hjelpe-linjer.
- **Separatorer** (`None` i menylisten) blir til `rumps.rumps.SeparatorMenuItem`, som IKKE eksponerer `.hidden` som public API. For de to separatorene som må kunne skjules dynamisk (`top_separator` mellom feil-blokk og datalinjer, `mid_separator` mellom sesjon- og uke-blokken), opprettes de eksplisitt som `rumps.rumps.SeparatorMenuItem()`-instanser (i stedet for `None`) slik at vi beholder en referanse, og skjules via `sep._menuitem.setHidden_(bool)` — samme underliggende mekanisme som `MenuItem.hidden` bruker internt. De øvrige, statiske separatorene (mellom «Varsle ved 90%»/«Avinstaller», og mellom «Avslutt»/footer) forblir vanlig `None` som i dag — de skal aldri skjules.
- **«Avslutt» må plasseres manuelt.** I dag sendes `quit_button=rumps.MenuItem("Avslutt")` til `rumps.App.__init__`, som gjør at rumps **automatisk legger «Avslutt» helt bakerst i menyen** (etter hele `self.menu`-listen, inkludert versjonslinjen) når `.run()` kalles — bekreftet ved å lese `rumps.py` sin `nsapplicationDidFinishLaunching_`. Det nye designet krever at «Avslutt» ligger rett under «Avinstaller», med footeren under igjen. Løsning: `quit_button=None` i `super().__init__`, og en egen `self.quit_item = rumps.MenuItem("Avslutt", callback=rumps.quit_application)` satt inn på riktig plass i `self.menu`-listen. `rumps.quit_application(sender=None)` er en fri funksjon som kan brukes direkte som callback (verifisert signatur).
- **Placeholder-titler ved opprettelse må være innbyrdes ulike** (verifisert empirisk): `rumps.Menu` er et dict internt, keyet på `MenuItem.title` ved opprettelsestidspunktet. Flere `MenuItem`-er opprettet med samme (f.eks. tomme) tittel kolliderer i dette interne dict-et — selve `NSMenu`-visningen påvirkes ikke (alle elementer vises fortsatt korrekt), men for å ikke basere oss på udokumentert kollisjonstoleranse, gis hvert element en distinkt, kortfattet placeholder-tittel ved opprettelse (blir uansett overskrevet av `refresh(None)` som kjøres synkront på slutten av `__init__`, akkurat som i dag).
- **Feltnavn `resets_at` verifisert mot ekte API-svar** i denne økten (`data["five_hour"]["resets_at"]` / `data["seven_day"]["resets_at"]`, ISO-8601 med UTC-offset). `datetime.fromisoformat` i Python 3.9 håndterer dette formatet uten problemer (testet).

---

### Task 1: Utvid `UsageData` med nullstillingstidspunkt

**Files:**
- Modify: `claude_usage/usage_client.py`
- Test: `tests/test_usage_client.py`

**Interfaces:**
- Produces: `UsageData.session_resets_at: datetime | None`, `UsageData.weekly_resets_at: datetime | None` — konsumeres av `format_reset` i Task 2/3.

- [ ] **Step 1: Skriv feilende tester**

Legg til i `tests/test_usage_client.py` (fixturen `_sample_response` har allerede `resets_at`-felt fra før — den delen er uendret):

```python
from datetime import datetime, timezone


@patch("claude_usage.usage_client.requests.get")
def test_fetch_usage_parses_reset_timestamps(mock_get):
    mock_get.return_value = _mock_response(200, _sample_response())

    usage = fetch_usage(cookie="session=abc", api_url=API_URL)

    assert usage.session_resets_at == datetime.fromisoformat(
        "2026-07-24T21:09:59.018485+00:00"
    )
    assert usage.weekly_resets_at == datetime.fromisoformat(
        "2026-07-28T19:59:59.018510+00:00"
    )


@patch("claude_usage.usage_client.requests.get")
def test_fetch_usage_handles_missing_resets_at(mock_get):
    response = _sample_response()
    del response["five_hour"]["resets_at"]
    del response["seven_day"]["resets_at"]
    mock_get.return_value = _mock_response(200, response)

    usage = fetch_usage(cookie="session=abc", api_url=API_URL)

    assert usage.session_resets_at is None
    assert usage.weekly_resets_at is None


@patch("claude_usage.usage_client.requests.get")
def test_fetch_usage_handles_invalid_resets_at(mock_get):
    response = _sample_response()
    response["five_hour"]["resets_at"] = "not-a-date"
    mock_get.return_value = _mock_response(200, response)

    usage = fetch_usage(cookie="session=abc", api_url=API_URL)

    assert usage.session_resets_at is None
```

Oppdater også de to eksisterende testene `test_fetch_usage_parses_percentages` og
`test_fetch_usage_rounds_float_percentages` sine `assert usage == UsageData(...)`-linjer
til å inkludere de to nye feltene, siden `UsageData` er `frozen=True` og
likhetssjekk nå krever alle felt:

```python
    assert usage == UsageData(
        session_percent=62,
        weekly_percent=58,
        session_resets_at=datetime.fromisoformat("2026-07-24T21:09:59.018485+00:00"),
        weekly_resets_at=datetime.fromisoformat("2026-07-28T19:59:59.018510+00:00"),
    )
```
(bruk samme mønster i begge testene — juster kun prosentverdiene, tidspunktene er de samme siden `_sample_response()` alltid bruker samme faste `resets_at`-verdier).

- [ ] **Step 2: Kjør testene og bekreft at de feiler**

Run: `venv/bin/python -m pytest tests/test_usage_client.py -v`
Expected: FAIL — `UsageData.__init__() missing 2 required positional arguments` eller `AttributeError: 'UsageData' object has no attribute 'session_resets_at'`.

- [ ] **Step 3: Implementer**

I `claude_usage/usage_client.py`, legg til import øverst:

```python
from datetime import datetime
```

Utvid dataklassen (linje 14-17):

```python
@dataclass(frozen=True)
class UsageData:
    session_percent: int
    weekly_percent: int
    session_resets_at: datetime | None
    weekly_resets_at: datetime | None
```

Erstatt `_parse_usage` (linje 48-51):

```python
def _parse_usage(data: dict) -> UsageData:
    session_percent = round(data["five_hour"]["utilization"])
    weekly_percent = round(data["seven_day"]["utilization"])
    return UsageData(
        session_percent=session_percent,
        weekly_percent=weekly_percent,
        session_resets_at=_parse_resets_at(data["five_hour"]),
        weekly_resets_at=_parse_resets_at(data["seven_day"]),
    )


def _parse_resets_at(section: dict) -> datetime | None:
    raw = section.get("resets_at")
    if not raw:
        return None
    try:
        return datetime.fromisoformat(raw)
    except (TypeError, ValueError):
        return None
```

- [ ] **Step 4: Kjør testene og bekreft at de passerer**

Run: `venv/bin/python -m pytest tests/test_usage_client.py -v`
Expected: PASS (alle tester, inkludert de tre nye og de to oppdaterte)

- [ ] **Step 5: Commit**

```bash
git add claude_usage/usage_client.py tests/test_usage_client.py
git commit -m "Utvid UsageData med nullstillingstidspunkt fra API-et"
```

---

### Task 2: Formateringsfunksjoner i `main.py`

**Files:**
- Modify: `claude_usage/main.py` (kun nye modulnivå-funksjoner + imports, ingen endring i `ClaudeUsageApp` ennå)
- Test: `tests/test_main.py`

**Interfaces:**
- Produces: `format_title(session: int, weekly: int, threshold: int) -> str`,
  `format_meter(label: str, percent: int, threshold: int) -> str`,
  `format_reset(resets_at: datetime | None, now: datetime) -> str | None`,
  `format_footer(version: str, updated_at: datetime | None) -> str` — konsumeres av `ClaudeUsageApp` i Task 3.

- [ ] **Step 1: Skriv feilende tester**

Legg til øverst i `tests/test_main.py`, ved siden av eksisterende import:

```python
from datetime import datetime, timedelta, timezone

import pytest

from claude_usage.main import format_footer, format_meter, format_reset, format_title

NOW = datetime(2026, 7, 24, 12, 33, 0, tzinfo=timezone.utc)


def _local_clock(dt: datetime) -> str:
    return f"{dt.astimezone():%H:%M}"


@pytest.mark.parametrize(
    "session, weekly, expected",
    [
        (43, 76, "43 · 76"),
        (43, 92, "43 · 92!"),
        (91, 92, "91! · 92!"),
    ],
)
def test_format_title(session, weekly, expected):
    assert format_title(session, weekly, threshold=90) == expected


@pytest.mark.parametrize(
    "percent, filled_cells, suffix",
    [
        (0, 0, ""),
        (4, 0, ""),
        (5, 0, ""),  # round(0.5) = 0 i Python (banker's rounding) — bevisst dokumentert her
        (43, 4, ""),
        (89, 9, ""),
        (90, 9, "!"),
        (100, 10, "!"),
        (112, 10, "!"),  # klippes til 10 celler, prosenttallet vises uklippet
    ],
)
def test_format_meter(percent, filled_cells, suffix):
    result = format_meter("Sesjon", percent, threshold=90)
    assert result == f"{'Sesjon':<7}{'▰' * filled_cells}{'▱' * (10 - filled_cells)} {percent}%{suffix}"


def test_format_reset_returns_none_when_missing():
    assert format_reset(None, NOW) is None


@pytest.mark.parametrize(
    "delta, expected_relative",
    [
        (timedelta(minutes=0), "nå"),
        (timedelta(minutes=-5), "nå"),  # tidspunkt i fortiden, klippes til «nå»
        (timedelta(minutes=47), "om 47 min"),
        (timedelta(hours=1, minutes=47), "om 1 t 47 min"),
    ],
)
def test_format_reset_under_24_hours(delta, expected_relative):
    resets_at = NOW + delta
    result = format_reset(resets_at, NOW)
    assert result == f"Nullstilles {_local_clock(resets_at)} ({expected_relative})"


def test_format_reset_over_24_hours_includes_weekday():
    resets_at = NOW + timedelta(days=3)  # NOW er en fredag => +3 dager = mandag
    result = format_reset(resets_at, NOW)
    local = resets_at.astimezone()
    weekday = ["man", "tir", "ons", "tor", "fre", "lør", "søn"][local.weekday()]
    assert result == f"Nullstilles {weekday} {local:%H:%M} (om 3 d)"


def test_format_footer_without_timestamp():
    assert format_footer("0.2.0", None) == "App v0.2.0"


def test_format_footer_with_timestamp():
    updated_at = datetime(2026, 7, 24, 12, 3, tzinfo=timezone.utc)
    assert format_footer("0.2.0", updated_at) == f"Oppdatert {_local_clock(updated_at)} · App v0.2.0"
```

- [ ] **Step 2: Kjør testene og bekreft at de feiler**

Run: `venv/bin/python -m pytest tests/test_main.py -v`
Expected: FAIL — `ImportError: cannot import name 'format_title' from 'claude_usage.main'`

- [ ] **Step 3: Implementer**

I `claude_usage/main.py`, endre importlinjene øverst (linje 1-9) til:

```python
import shutil
import subprocess
from datetime import datetime, timedelta, timezone
from pathlib import Path

import rumps

from claude_usage import __version__, config
from claude_usage.notifier import THRESHOLD_PERCENT, ThresholdNotifier
from claude_usage.usage_client import UsageAuthError, UsageData, UsageFetchError, fetch_usage
```

Legg til disse konstantene og funksjonene rett under de eksisterende modulkonstantene (etter `APP_BUNDLE_PATH`-linjen):

```python
METER_CELLS = 10
METER_FILLED = "▰"
METER_EMPTY = "▱"
WEEKDAY_ABBREVIATIONS = ["man", "tir", "ons", "tor", "fre", "lør", "søn"]


def format_title(session: int, weekly: int, threshold: int) -> str:
    session_str = f"{session}!" if session >= threshold else f"{session}"
    weekly_str = f"{weekly}!" if weekly >= threshold else f"{weekly}"
    return f"{session_str} · {weekly_str}"


def format_meter(label: str, percent: int, threshold: int) -> str:
    filled = max(0, min(METER_CELLS, round(percent / 10)))
    bar = METER_FILLED * filled + METER_EMPTY * (METER_CELLS - filled)
    suffix = "!" if percent >= threshold else ""
    return f"{label:<7}{bar} {percent}%{suffix}"


def format_reset(resets_at: datetime | None, now: datetime) -> str | None:
    if resets_at is None:
        return None

    local = resets_at.astimezone()
    delta = resets_at - now

    if delta <= timedelta(minutes=1):
        return f"Nullstilles {local:%H:%M} (nå)"
    if delta < timedelta(hours=1):
        minutes = int(delta.total_seconds() // 60)
        return f"Nullstilles {local:%H:%M} (om {minutes} min)"
    if delta < timedelta(hours=24):
        total_minutes = int(delta.total_seconds() // 60)
        hours, minutes = divmod(total_minutes, 60)
        return f"Nullstilles {local:%H:%M} (om {hours} t {minutes} min)"

    weekday = WEEKDAY_ABBREVIATIONS[local.weekday()]
    days = round(delta.total_seconds() / 86400)
    return f"Nullstilles {weekday} {local:%H:%M} (om {days} d)"


def format_footer(version: str, updated_at: datetime | None) -> str:
    if updated_at is None:
        return f"App v{version}"
    return f"Oppdatert {updated_at.astimezone():%H:%M} · App v{version}"
```

(Disse er rene funksjoner uten `self`/I/O, plassert på modulnivå — de brukes av
`ClaudeUsageApp` først i Task 3, så importen av `THRESHOLD_PERCENT` og `UsageData`
brukes ikke ennå i denne oppgaven, men legges inn nå siden importlinjen uansett
må endres. `ClaudeUsageApp`-klassen forblir uendret i denne oppgaven.)

- [ ] **Step 4: Kjør testene og bekreft at de passerer**

Run: `venv/bin/python -m pytest tests/test_main.py -v`
Expected: PASS (alle nye parametriserte tester + eksisterende `test_menu_shows_version`)

- [ ] **Step 5: Kjør hele testsuiten**

Run: `venv/bin/python -m pytest`
Expected: Alle tester passerer (ingen regresjon i `test_config.py`, `test_notifier.py`, `test_usage_client.py`). `ClaudeUsageApp` er ikke endret ennå, så appen fungerer som før.

- [ ] **Step 6: Commit**

```bash
git add claude_usage/main.py tests/test_main.py
git commit -m "Legg til formateringsfunksjoner for ny menylinje-UI"
```

---

### Task 3: Meny-restrukturering i `ClaudeUsageApp`

**Files:**
- Modify: `claude_usage/main.py`
- Test: `tests/test_main.py`

**Interfaces:**
- Consumes: `format_title`, `format_meter`, `format_reset`, `format_footer` (Task 2), `UsageData.session_resets_at`/`weekly_resets_at` (Task 1), `THRESHOLD_PERCENT` fra `claude_usage.notifier`.
- Produces: `ClaudeUsageApp` med attributtene `error_item`, `help_item`, `top_separator`,
  `session_meter_item`, `session_reset_item`, `mid_separator`, `weekly_meter_item`,
  `weekly_reset_item`, `notifications_item`, `uninstall_item`, `quit_item`,
  `footer_item`, `_last_usage`, `_last_updated_at` — ingen senere oppgave i denne
  planen bruker disse, men navnene er endelige for appens egen kode.

- [ ] **Step 1: Skriv feilende tester**

Legg til i `tests/test_main.py` (i tillegg til det som ble lagt til i Task 2):

```python
from claude_usage.config import CredentialsMissingError
from claude_usage.usage_client import UsageAuthError, UsageData


def _usage(session=43, weekly=76, session_resets=None, weekly_resets=None):
    return UsageData(
        session_percent=session,
        weekly_percent=weekly,
        session_resets_at=session_resets,
        weekly_resets_at=weekly_resets,
    )


def test_normal_state_shows_data_and_hides_error():
    with patch("claude_usage.main.config.load_credentials", return_value=("c", "u")), \
         patch("claude_usage.main.fetch_usage", return_value=_usage()):
        app = ClaudeUsageApp()

    assert app.title == "43 · 76"
    assert app.error_item.hidden is True
    assert app.help_item.hidden is True
    assert app.session_meter_item.hidden is False
    assert app.session_meter_item.title.startswith("Sesjon")
    assert app.weekly_meter_item.hidden is False
    assert app.footer_item.title.startswith("Oppdatert ")
    assert app.footer_item.title.endswith(f"App v{__version__}")


def test_normal_state_hides_reset_line_when_resets_at_missing():
    with patch("claude_usage.main.config.load_credentials", return_value=("c", "u")), \
         patch("claude_usage.main.fetch_usage", return_value=_usage()):
        app = ClaudeUsageApp()

    assert app.session_reset_item.hidden is True
    assert app.weekly_reset_item.hidden is True


def test_threshold_crossed_marks_title_and_meter():
    with patch("claude_usage.main.config.load_credentials", return_value=("c", "u")), \
         patch("claude_usage.main.fetch_usage", return_value=_usage(weekly=92)):
        app = ClaudeUsageApp()

    assert app.title == "43 · 92!"
    assert app.weekly_meter_item.title.endswith("92%!")


def test_error_state_before_any_fetch_hides_all_data_lines():
    with patch(
        "claude_usage.main.config.load_credentials",
        side_effect=CredentialsMissingError("mangler"),
    ):
        app = ClaudeUsageApp()

    assert app.title == "⚠️"
    assert app.error_item.hidden is False
    assert app.error_item.title == "mangler"
    assert app.help_item.hidden is True
    assert app.session_meter_item.hidden is True
    assert app.weekly_meter_item.hidden is True
    assert app.footer_item.title == f"App v{__version__}"


def test_error_state_with_prior_data_shows_stale_meters_without_resets():
    responses = [_usage(session_resets=datetime.now(timezone.utc) + timedelta(hours=1))]

    def fetch_side_effect(*args, **kwargs):
        if responses:
            return responses.pop()
        raise UsageAuthError("expired")

    with patch("claude_usage.main.config.load_credentials", return_value=("c", "u")), \
         patch("claude_usage.main.fetch_usage", side_effect=fetch_side_effect):
        app = ClaudeUsageApp()
        assert app.session_reset_item.hidden is False  # sanity check før feilen inntreffer

        app.refresh(None)  # andre kall: responses er tom, kaster UsageAuthError

    assert app.title == "⚠️"
    assert app.error_item.hidden is False
    assert app.error_item.title == "Cookien utløpt – oppdater"
    assert app.help_item.hidden is False
    assert app.help_item.title.startswith("Klikk for oppskrift · siste tall ")
    assert app.session_meter_item.hidden is False  # gamle tall vises fortsatt
    assert app.session_reset_item.hidden is True  # men uten nullstillingslinje
    assert app.footer_item.title.startswith("Oppdatert ")
```

- [ ] **Step 2: Kjør testene og bekreft at de feiler**

Run: `venv/bin/python -m pytest tests/test_main.py -v`
Expected: FAIL — `AttributeError: 'ClaudeUsageApp' object has no attribute 'session_meter_item'` (eller lignende for de andre nye attributtene).

- [ ] **Step 3: Implementer — erstatt hele `ClaudeUsageApp`-klassen**

Erstatt hele klassekroppen (fra `class ClaudeUsageApp(rumps.App):` til rett før
`def main() -> None:`) med:

```python
class ClaudeUsageApp(rumps.App):
    def __init__(self):
        super().__init__(
            "Claude Usage",
            title="…",
            quit_button=None,
        )
        self.settings = config.load_settings()
        self.notifier = ThresholdNotifier()
        self._last_usage: UsageData | None = None
        self._last_updated_at: datetime | None = None

        self.error_item = rumps.MenuItem("Feil", callback=None)
        self.error_item.hidden = True
        self.help_item = rumps.MenuItem("Hjelp", callback=None)
        self.help_item.hidden = True
        self.top_separator = rumps.rumps.SeparatorMenuItem()
        self.top_separator._menuitem.setHidden_(True)

        self.session_meter_item = rumps.MenuItem("Sesjon-måler", callback=None)
        self.session_meter_item.hidden = True
        self.session_reset_item = rumps.MenuItem("Sesjon-nullstilling", callback=None)
        self.session_reset_item.hidden = True
        self.mid_separator = rumps.rumps.SeparatorMenuItem()
        self.mid_separator._menuitem.setHidden_(True)
        self.weekly_meter_item = rumps.MenuItem("Uke-måler", callback=None)
        self.weekly_meter_item.hidden = True
        self.weekly_reset_item = rumps.MenuItem("Uke-nullstilling", callback=None)
        self.weekly_reset_item.hidden = True

        self.notifications_item = rumps.MenuItem(
            "Varsle ved 90%", callback=self._toggle_notifications
        )
        self.notifications_item.state = self.settings.notifications_enabled
        self.uninstall_item = rumps.MenuItem("Avinstaller", callback=self._uninstall)
        self.quit_item = rumps.MenuItem("Avslutt", callback=rumps.quit_application)
        self.footer_item = rumps.MenuItem(format_footer(__version__, None), callback=None)

        self.menu = [
            self.error_item,
            self.help_item,
            self.top_separator,
            self.session_meter_item,
            self.session_reset_item,
            self.mid_separator,
            self.weekly_meter_item,
            self.weekly_reset_item,
            None,
            self.notifications_item,
            None,
            self.uninstall_item,
            self.quit_item,
            None,
            self.footer_item,
        ]

        self.timer = rumps.Timer(self.refresh, REFRESH_INTERVAL_SECONDS)
        self.timer.start()

        self.refresh(None)

    def _toggle_notifications(self, sender) -> None:
        self.settings.notifications_enabled = not self.settings.notifications_enabled
        sender.state = self.settings.notifications_enabled
        config.save_settings(self.settings)

    def refresh(self, _sender) -> None:
        try:
            cookie, api_url = config.load_credentials()
            usage = fetch_usage(cookie, api_url)
        except config.CredentialsMissingError as exc:
            self._show_error(str(exc), actionable=True)
            return
        except UsageAuthError:
            self._show_error("Cookien utløpt – oppdater", actionable=True)
            return
        except UsageFetchError as exc:
            self._show_error(str(exc), actionable=False)
            return
        except Exception as exc:
            self._show_error(f"Uventet feil: {exc}", actionable=False)
            return

        now = datetime.now(timezone.utc)
        self._last_usage = usage
        self._last_updated_at = now

        self.title = format_title(usage.session_percent, usage.weekly_percent, THRESHOLD_PERCENT)
        self.error_item.hidden = True
        self.help_item.hidden = True
        self.top_separator._menuitem.setHidden_(True)
        self._render_data_items(usage, now)
        self.footer_item.title = format_footer(__version__, now)

        self.notifier.check(
            usage.session_percent, usage.weekly_percent, self.settings.notifications_enabled
        )

    def _show_error(self, message: str, actionable: bool = False) -> None:
        self.title = "⚠️"
        self.error_item.hidden = False
        self.error_item.title = message
        self.error_item.set_callback(self._show_help if actionable else None)

        if self._last_usage is None:
            self.help_item.hidden = True
            self.top_separator._menuitem.setHidden_(True)
            self._hide_data_items()
        else:
            updated_str = f"{self._last_updated_at.astimezone():%H:%M}"
            prefix = "Klikk for oppskrift · " if actionable else ""
            self.help_item.title = f"{prefix}siste tall {updated_str}"
            self.help_item.hidden = False
            self.top_separator._menuitem.setHidden_(False)
            self._render_data_items_compact(self._last_usage)

        self.footer_item.title = format_footer(__version__, self._last_updated_at)

    def _render_data_items(self, usage: UsageData, now: datetime) -> None:
        self.session_meter_item.hidden = False
        self.session_meter_item.title = format_meter("Sesjon", usage.session_percent, THRESHOLD_PERCENT)
        self.weekly_meter_item.hidden = False
        self.weekly_meter_item.title = format_meter("Uke", usage.weekly_percent, THRESHOLD_PERCENT)

        session_reset = format_reset(usage.session_resets_at, now)
        self.session_reset_item.hidden = session_reset is None
        if session_reset is not None:
            self.session_reset_item.title = session_reset

        weekly_reset = format_reset(usage.weekly_resets_at, now)
        self.weekly_reset_item.hidden = weekly_reset is None
        if weekly_reset is not None:
            self.weekly_reset_item.title = weekly_reset

        self.mid_separator._menuitem.setHidden_(False)

    def _render_data_items_compact(self, usage: UsageData) -> None:
        self.session_meter_item.hidden = False
        self.session_meter_item.title = format_meter("Sesjon", usage.session_percent, THRESHOLD_PERCENT)
        self.weekly_meter_item.hidden = False
        self.weekly_meter_item.title = format_meter("Uke", usage.weekly_percent, THRESHOLD_PERCENT)
        self.session_reset_item.hidden = True
        self.weekly_reset_item.hidden = True
        self.mid_separator._menuitem.setHidden_(True)

    def _hide_data_items(self) -> None:
        self.session_meter_item.hidden = True
        self.session_reset_item.hidden = True
        self.weekly_meter_item.hidden = True
        self.weekly_reset_item.hidden = True
        self.mid_separator._menuitem.setHidden_(True)

    def _uninstall(self, _sender) -> None:
        response = rumps.alert(
            title="Avinstaller Claude Usage",
            message=(
                "Dette fjerner autostart-oppsettet (LaunchAgent), app-ikonet "
                "i ~/Applications, og lagrede innstillinger. Prosjektmappen "
                "og .env beholdes. Appen avsluttes etterpå."
            ),
            ok="Avinstaller",
            cancel="Avbryt",
        )
        if response != 1:
            return

        plist_existed = LAUNCH_AGENT_PLIST_PATH.exists()
        if plist_existed:
            LAUNCH_AGENT_PLIST_PATH.unlink()

        if APP_BUNDLE_PATH.exists():
            shutil.rmtree(APP_BUNDLE_PATH)

        if config.CONFIG_DIR.exists():
            shutil.rmtree(config.CONFIG_DIR)

        rumps.alert(
            title="Avinstallert",
            message="LaunchAgent, app-ikon og lagrede innstillinger er fjernet.",
        )

        if plist_existed:
            subprocess.run(
                ["launchctl", "remove", LAUNCH_AGENT_LABEL],
                capture_output=True,
            )
        rumps.quit_application()

    def _show_help(self, _sender) -> None:
        subprocess.run(["open", "-e", str(config.env_file_path())])
        rumps.alert(
            title="Slik henter du en fersk cookie",
            message=(
                "1. Åpne claude.ai/settings/usage i nettleseren\n"
                "2. Åpne utviklerverktøy (⌘+⌥+I) → Network-fanen\n"
                "3. Last siden på nytt, klikk på \"usage\"-forespørselen\n"
                "4. Under Headers → Request Headers: kopier hele Cookie-verdien\n"
                "5. Lim inn i CLAUDE_USAGE_COOKIE i .env-filen som nettopp åpnet seg\n"
                "6. Lagre filen — appen henter automatisk på nytt innen ett minutt"
            ),
        )
```

Merk: `_uninstall` og `_show_help` er identiske til i dag bortsett fra siste
linje i `_show_help`-dialogen (endret per «Avvik»-seksjonen over). `refresh_item`
og `version_item` finnes ikke lenger — erstattet av `footer_item`.

- [ ] **Step 4: Kjør testene og bekreft at de passerer**

Run: `venv/bin/python -m pytest tests/test_main.py -v`
Expected: PASS — alle tester, inkludert `test_menu_shows_version` (denne testen
refererer ikke lenger til `version_item`, se Step 4b) og de nye state-testene.

**Step 4b:** `test_menu_shows_version` i eksisterende `tests/test_main.py` bruker
`app.version_item.title == f"v{__version__}"` — dette attributtet finnes ikke
lenger. Erstatt testen med:

```python
def test_footer_shows_version():
    with patch(
        "claude_usage.main.config.load_credentials",
        side_effect=CredentialsMissingError("mangler"),
    ):
        app = ClaudeUsageApp()

    assert app.footer_item.title == f"App v{__version__}"
```

- [ ] **Step 5: Kjør hele testsuiten**

Run: `venv/bin/python -m pytest`
Expected: Alle tester passerer (`test_config.py`, `test_notifier.py`, `test_usage_client.py`, `test_main.py`).

- [ ] **Step 6: Manuell verifisering i den faktiske, kjørende appen**

```bash
launchctl kickstart -k gui/$(id -u)/local.claude-usage
```

Åpne dropdown-menyen i menylinjen og bekreft visuelt:
- Menylinjetittel viser `XX · YY` (ingen `%`-tegn)
- Sesjon- og uke-målere med `▰`/`▱`-stolper og nullstillingslinjer under hver
- Ingen «Oppdater nå»-linje
- «Avslutt» ligger rett under «Avinstaller», med en skillelinje og footeren
  (`Oppdatert HH:MM · App v0.1.0`) under
- Meny lukker seg IKKE av seg selv i løpet av de neste ~70 sekundene (venter
  gjennom minst én automatisk refresh mens menyen er åpen, for å bekrefte at
  `refresh` kun oppdaterer `title`/`hidden` og ikke bygger om menyen)

For å se feiltilstanden: åpne `.env` og sett en ugyldig cookie midlertidig,
kjør `launchctl kickstart -k gui/$(id -u)/local.claude-usage` på nytt, bekreft
at feilmeldingen + hjelpelinje vises øverst, målerne vises uten
nullstillingslinjer, og footeren fortsatt viser forrige kjente
oppdateringstidspunkt. Sett tilbake riktig cookie og kickstart igjen etterpå.

- [ ] **Step 7: Commit**

```bash
git add claude_usage/main.py tests/test_main.py
git commit -m "Restrukturer menylinje-UI: datalinjer, nullstilling og ny footer"
```

---

### Task 4: Dokumentasjon

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Oppdater `README.md`**

Erstatt eksempelblokken (linje 9-11, `S:40% U:15%`) med:

````markdown
```
43 · 76
```
````

I «Bruk»-seksjonen: fjern punktet om «Oppdater nå» (finnes ikke lenger), og
legg til en kort beskrivelse av menyinnholdet. Erstatt eksisterende
punktliste under `## Bruk` med:

```markdown
- **Menylinjen** viser sesjon- og uke-forbruk side ved side (f.eks. `43 · 76`),
  med `!` bak et tall som har passert 90 %, eller ⚠️ ved feil (utløpt cookie,
  nettverksfeil e.l.)
- **Dropdown-menyen** viser begge grensene som tekstmålere
  (`Sesjon  ▰▰▰▰▱▱▱▱▱▱ 43%`) med nullstillingstidspunkt under hver, appen
  oppdaterer seg selv hvert minutt
- **Varsle ved 90%** — av/på-bryter for macOS-notifikasjon når en grense
  nærmer seg fullt brukt
- **Avinstaller** — fjerner LaunchAgent-en, app-ikonet og lagrede
  innstillinger (spør om bekreftelse først; selve prosjektmappen og
  `.env` beholdes)
```

- [ ] **Step 2: Oppdater `CHANGELOG.md`**

Legg til under `## [Unreleased]`:

```markdown
### Changed
- Menylinjetittel forkortet til `sesjon · uke` (uten `%`), med `!`-markør
  ved ≥ 90 % — se `docs/design_handoff_menubar_1b/`
- Dropdown-menyen viser nå sesjon- og ukeforbruk som tekstmålere med
  nullstillingstidspunkt, i stedet for kun i menylinjetittelen
- Feiltilstanden viser siste kjente tall (uten nullstillingstidspunkt) i
  stedet for å skjule dem helt
- Footeren viser nå tidspunkt for siste vellykkede oppdatering sammen med
  versjonsnummeret (`Oppdatert 14:03 · App v0.1.0`)

### Removed
- «Oppdater nå»-menyvalget (appen oppdaterer seg selv hvert 60. sekund)
```

- [ ] **Step 3: Commit**

```bash
git add README.md CHANGELOG.md
git commit -m "Oppdater dokumentasjon for ny menylinje-UI"
```

---

### Task 5: Kutt release 0.2.0

**Files:**
- Ingen nye — kjører `scripts/release.sh` (fra tidligere versjonerings-arbeid) mot innholdet fra Task 1-4.

**OBS:** Dette steget pusher til `origin/main` og oppretter en offentlig
GitHub Release. Bekreft med Atle før scriptet kjøres.

- [ ] **Step 1: Push commits fra Task 1-4**

```bash
git push origin main
```

- [ ] **Step 2: Kjør release-scriptet**

```bash
./scripts/release.sh 0.2.0
```

Expected: Flytter `Unreleased`-innholdet til `## [0.2.0] - <dagens dato>` i
`CHANGELOG.md`, setter `__version__ = "0.2.0"` i `claude_usage/__init__.py`,
committer, tagger `v0.2.0`, pusher, og oppretter en GitHub Release.

- [ ] **Step 3: Verifiser**

```bash
git log --oneline -3
git tag
gh release view v0.2.0
launchctl kickstart -k gui/$(id -u)/local.claude-usage
```

Åpne menyen og bekreft at footeren nå viser `App v0.2.0`.

---

## Self-Review

- **Spec-dekning:** Tittelformat → Task 2/3. Målere + nullstilling → Task 1/2/3.
  Feiltilstand med gamle tall → Task 3. Footer → Task 2/3 (med avklart avvik).
  «Oppdater nå» fjernet → Task 3 (avklart avvik). `THRESHOLD_PERCENT` gjenbrukt
  → Task 2/3 (ingen hardkoding av 90). Dokumentasjon → Task 4. Release →
  Task 5. Alt fra handoff-dokumentet (justert for de tre avklarte avvikene)
  er dekket.
- **Placeholder-skann:** Ingen TBD/TODO — all kode er fullstendig utskrevet.
- **Typekonsistens:** `format_reset(resets_at, now)` brukes likt i Task 2 sine
  tester og Task 3 sin `_render_data_items`. `UsageData.session_resets_at`/
  `weekly_resets_at` (Task 1) matcher feltnavnene brukt i Task 3. Attributtnavn
  (`session_meter_item` osv.) er konsistente mellom Task 3 sin implementasjon
  og dens egne tester (ingen andre oppgaver refererer til disse attributtene).
